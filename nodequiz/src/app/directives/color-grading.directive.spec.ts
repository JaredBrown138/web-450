import { ColorGradingDirective } from './color-grading.directive';
import { ElementRef } from '@angular/core';

describe('ColorGradingDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorGradingDirective();
    expect(directive).toBeTruthy();
  });
});
